<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="@color/white"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="16dp">

        <!-- 1. AVATAR / ICONO (Izquierda) -->
        <FrameLayout
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:background="@drawable/circle_background">
            <!-- Nota: Usamos el mismo circle_background pero le cambiamos el tinte abajo
                 O simplemente usamos el icono grande si no quieres el círculo de fondo -->

            <ImageView
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_person"
                app:tint="@color/orange_coach"
                android:contentDescription="Avatar Entrenador"/>
        </FrameLayout>

        <!-- 2. INFORMACIÓN (Centro) -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="8dp">

            <!-- Nombre -->
            <TextView
                android:id="@+id/tvCoachName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Roberto Gómez"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@color/gray_dark"/>

            <!-- Deporte (Color Orange Coach para destacar) -->
            <TextView
                android:id="@+id/tvCoachSport"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Entrenador de Básquetbol"
                android:textSize="13sp"
                android:textStyle="bold"
                android:textColor="@color/orange_coach"
                android:layout_marginBottom="6dp"/>

            <!-- Teléfono -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginBottom="2dp">

                <ImageView
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:src="@android:drawable/ic_menu_call"
                    app:tint="@color/gray_text"
                    android:layout_marginEnd="6dp"/>

                <TextView
                    android:id="@+id/tvCoachPhone"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    tools:text="55 1234 5678"
                    android:textSize="12sp"
                    android:textColor="@color/gray_text"/>
            </LinearLayout>

            <!-- Correo -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <ImageView
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:src="@android:drawable/ic_dialog_email"
                    app:tint="@color/gray_text"
                    android:layout_marginEnd="6dp"/>

                <TextView
                    android:id="@+id/tvCoachEmail"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    tools:text="roberto@uaemex.mx"
                    android:textSize="12sp"
                    android:textColor="@color/gray_text"
                    android:ellipsize="end"
                    android:maxLines="1"/>
            </LinearLayout>
        </LinearLayout>

        <!-- 3. ACCIÓN RÁPIDA (Derecha) -->
        <!-- Botón para llamar directamente -->
        <ImageView
            android:id="@+id/btnCallAction"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="10dp"
            android:src="@android:drawable/ic_menu_call"
            app:tint="@color/green_primary"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:contentDescription="Llamar al entrenador"/>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>